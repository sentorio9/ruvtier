const WatermarkLogo = () => (
  <div
    className="fixed inset-0 z-0 flex items-center justify-center pointer-events-none select-none"
    aria-hidden="true"
  >
    <span className="luxury-heading-lg !text-[clamp(60px,10vw,140px)] tracking-[0.3em] opacity-[0.04]">
      RUVTIER
    </span>
  </div>
);

export default WatermarkLogo;
